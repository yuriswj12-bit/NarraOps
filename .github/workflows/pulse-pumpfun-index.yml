name: Pulse Pump.fun Index

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pulse-pumpfun-index
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Explain retired collector
        run: echo "Dune collection is retired. Direct Solana collection replaces this workflow."
